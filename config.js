const API_CONFIG = {
  // When hosting on Vercel with vercel-php, both frontend and backend are on the same domain.
  // Leaving this empty ensures fetch calls use relative paths (e.g., /get_reviews.php).
  BASE_URL: "",
};
